apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: local-path
    namespace: argocd
spec:
    project: default

    source:
        repoURL: https://github.com/Ndomi-Nqeketo/argocd.git
        targetRevision: main
        path: applications/local-path

    destination:
        server: https://kubernetes.default.svc
        namespace: local-path-storage

    syncPolicy:
        automated:
            prune: true
            selfHeal: true
        syncOptions:
            - CreateNamespace=true